version: '3.8'

services:
  sync-checker:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./sync_checker.db:/app/sync_checker.db
      - ./public:/app/public
    restart: unless-stopped
